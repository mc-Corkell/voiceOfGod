<%= form_for ([@slam, @slam.participants.build]) do |f| %>

<% if @slam.errors.any? %>
   <div id="error_explanation">
      <h2><%= pluralize(@slam.errors.count, "error") %> prohibited this participant from being saved:</h2>

      <ul>
      <% @slam.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	
<div class="field">
    <%= f.label :letterOrder, "Order" %><br />
    <%= f.text_field :letterOrder %>
</div>
 <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
